/* XPECT_SETUP eu.numberfour.n4jsx.spec.tests.N4JSXSpecTest

	ResourceSet {
		ThisFile {}
		File "react.n4jsd" {}
	}

	END_SETUP
*/


import * as React from "react"

class C extends React.Component<~Object with {prop: int}, Object> {
	@Override
	public render(): React.Element {
		return <button/>
	}
}

//XPECT errors --> "This is used as a React component but is neither function nor class, but of type [unknown] instead." at "MyProp"
<MyProp/>

const D = 10;
//XPECT errors --> "This is used as a React component but is neither function nor class, but of type int instead." at "D"
<D/>
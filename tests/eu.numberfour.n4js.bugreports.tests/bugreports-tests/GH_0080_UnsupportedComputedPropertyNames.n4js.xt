/* XPECT_SETUP eu.numberfour.n4js.bugreports.tests.N4JSBugreportTest END_SETUP  */


let some: any+;

class C {
	// XPECT errors --> "Not a compile-time expression: only references to enum literals or built-in symbols allowed." at "some.field"
	[some.field];

	// XPECT errors --> "Not a compile-time expression: a call expression is never a compile-time expression." at "anotherField()"
	[anotherField()];
}

function anotherField(): string {
	return null;
}


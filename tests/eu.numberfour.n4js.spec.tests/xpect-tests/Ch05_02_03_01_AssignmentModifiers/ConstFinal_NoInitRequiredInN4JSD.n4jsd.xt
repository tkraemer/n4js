/* XPECT_SETUP eu.numberfour.n4js.spec.tests.N4JSSpecTest END_SETUP */

export external public class A {
	// XPECT noerrors --> "Const field c must be provided with an initializer." at "c"
	const c;
	// XPECT noerrors --> "Final field f must be provided with an initializer or must be initialized in the constructor." at "f"
	@Final f;
}

// XPECT noerrors --> "Const variable v must be provided with an initializer." at "v"
export external public const v;


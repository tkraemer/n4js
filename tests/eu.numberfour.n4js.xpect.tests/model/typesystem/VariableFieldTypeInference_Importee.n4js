

export public class Remote {
	field = "hello";
	static staticField = "hello";
	
	// without initialization expression:
	anyField;
	static anyStaticField;
}


export var remote_variable = "world";


// some other types that are inferred to any (will be supported later):

export var remote_fun = function(p : string) : number { return 0; };
export var remote_obj = { name: "Alice" };

